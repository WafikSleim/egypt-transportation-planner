<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Read on demand only, when the passenger taps "my location". There is
         deliberately no ACCESS_BACKGROUND_LOCATION here: following a trip is
         a separate feature with its own consent and its own persistent
         notification, and it must not arrive by accident through this. -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <!-- The app plans trips, it does not require a fix. A phone with no GPS
         hardware must still be able to install it and pick stops by name. -->
    <uses-feature android:name="android.hardware.location.gps" android:required="false" />

    <!-- Android 13+ runtime permission, asked in context and never at launch -
         see core/notifications/notification_permission_prompt.dart. Declared
         here as well as in the plugin's own manifest so that the one
         permission this app shows a dialog for is visible in this file.

         There is deliberately no SCHEDULE_EXACT_ALARM or USE_EXACT_ALARM: a
         departure reminder tolerates a minute either side of its lead time,
         those permissions are audited by Play as alarm-clock features, and
         asking for one this app does not need is how a review is refused.
         Reminders are scheduled inexactly - see LocalNotificationService. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- A reminder is scheduled with the OS, and a reboot clears the OS's
         alarm table. Without this, a reminder set the night before is
         silently gone by morning - which looks exactly like an app that
         works. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:label="@string/app_name"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- flutter_local_notifications' own receivers. The plugin stopped
             declaring these itself at version 16, so an app that schedules
             anything has to list them: without the first two a scheduled
             reminder never fires and never survives a reboot, and without
             the third the "turn these off" action on a notification does
             nothing. All three fail quietly, which is why they are here with
             a comment rather than copied in silently. -->
        <receiver android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver" />
        <receiver android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>
        <receiver android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ActionBroadcastReceiver" />

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
